# Supervision avec Uptime Kuma

## Objectif

L'objectif de cette étape est de mettre en place une supervision simple de la plateforme GLPI.

Uptime Kuma permet de surveiller la disponibilité d'un service web et d'identifier rapidement une interruption.

## Service supervisé

Le service supervisé est l'application GLPI, accessible depuis la VM à l'adresse :

```text
http://localhost:8080
